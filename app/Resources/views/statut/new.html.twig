{% extends 'layout_admin.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block content %}
    <div class="container">
        <h1>Création d'un statut</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create"/>
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('admin_management') }}">Retour à la liste des statuts</a>
            </li>
        </ul>
    </div>
{% endblock %}
